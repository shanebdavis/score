import &ArtSuite, &Components

class App extends FluxComponent
  @subscriptions :navState.navStack

  render: ->
    log :RENDER_APP
    {pageName, props} = peek @navStack
    log {pageName, props}
    CanvasElement
      RectangleElement
        color: &Palette.background

      Element
        padding: 10
        Pages[pageName]? props
